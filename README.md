# SwiftCart E-Commerce Assignment

## 📱 প্রকল্পের বর্ণনা
SwiftCart হল একটি সম্পূর্ণ কার্যকারী ই-কমার্স অ্যাপ্লিকেশন যা Fake Store API থেকে ডিনামিকভাবে পণ্য এবং বিভাগ লোড করে। এটি Vanilla JavaScript, HTML এবং Tailwind CSS দিয়ে তৈরি।

---

## 🎯 প্রকল্পের বৈশিষ্ট্য

### ✅ সম্পন্ন বৈশিষ্ট্যসমূহ:
1. **ডায়নামিক ক্যাটাগরি লোডিং** - API থেকে বিভাগ লোড করা হয় এবং ফিল্টার বাটন হিসাবে প্রদর্শিত হয়
2. **পণ্য গ্রিড ডিসপ্লে** - 4-কলাম রেসপন্সিভ লেআউটে পণ্য প্রদর্শন
3. **পণ্য কার্ড** - ছবি, শিরোনাম, মূল্য, বিভাগ ব্যাজ, রেটিং এবং বোতাম সহ
4. **বিস্তারিত মোডাল** - পূর্ণ পণ্য তথ্য মোডালে প্রদর্শন
5. **শপিং কার্ট** - পণ্য যোগ করা, সরানো এবং মোট গণনা করা
6. **লোডিং স্পিনার** - API ফেচের সময় লোডিং সূচক
7. **লোকাল স্টোরেজ** - কার্ট ডেটা সংরক্ষণ করা
8. **মোবাইল রেসপন্সিভ** - সমস্ত ডিভাইসে সামঞ্জস্যপূর্ণ
9. **নিউজলেটার সাবস্ক্রিপশন** - ইমেল সাবস্ক্রিপশন ফর্ম
10. **ফুটার** - সামাজিক লিঙ্ক এবং দ্রুত নেভিগেশন সহ

---

## 📚 জাভাস্ক্রিপ্ট প্রশ্নাবলীর উত্তর

### ১) `null` এবং `undefined` এর মধ্যে পার্থক্য কি?

**উত্তর:**

`null` এবং `undefined` উভয়ই খালি বা অসংজ্ঞায়িত মান প্রতিনিধিত্ব করে, তবে তাদের মধ্যে গুরুত্বপূর্ণ পার্থক্য রয়েছে:

- **`undefined`**: এটি জাভাস্ক্রিপ্ট দ্বারা স্বয়ংক্রিয়ভাবে সেট করা হয় যখন কোনো ভেরিয়েবল ঘোষণা করা হয় কিন্তু কোনো মূল্য নিয়োগ করা হয় না। এটি কোনো ফাংশনের ডিফল্ট রিটার্ন মূল্যও। উদাহরণ: `let x;` - এখানে x হল `undefined`।

- **`null`**: এটি একটি ইচ্ছাকৃত মূল্য যা প্রোগ্রামারকে অবশ্যই নির্ধারণ করতে হবে। এটি ইঙ্গিত করে যে কোনো মূল্য নেই। উদাহরণ: `let y = null;`

**তুলনা:**
```javascript
let x; // undefined
let y = null; // null
console.log(x == y);  // true (loose equality)
console.log(x === y); // false (strict equality)
```

---

### ২) জাভাস্ক্রিপ্টে `map()` ফাংশনের ব্যবহার কি? এটি `forEach()` থেকে কিভাবে আলাদা?

**উত্তর:**

`map()` এবং `forEach()` উভয়ই অ্যারের উপর পুনরাবৃত্তি করে, কিন্তু গুরুত্বপূর্ণ পার্থক্য আছে:

- **`map()`**: এটি একটি নতুন অ্যারে তৈরি করে যেখানে প্রতিটি উপাদান পরিবর্তিত হয়েছে। এটি একটি নতুন অ্যারে রিটার্ন করে।

- **`forEach()`**: এটি অ্যারের প্রতিটি উপাদানে একটি ফাংশন চালায় কিন্তু কোনো নতুন অ্যারে রিটার্ন করে না। এটি পার্শ্ব প্রতিক্রিয়ার জন্য ব্যবহৃত হয়।

**উদাহরণ:**
```javascript
const numbers = [1, 2, 3, 4];

// map() - নতুন অ্যারে তৈরি করে
const doubled = numbers.map(num => num * 2);
console.log(doubled); // [2, 4, 6, 8]

// forEach() - কোনো নতুন অ্যারে তৈরি করে না
let sum = 0;
numbers.forEach(num => {
    sum += num; // পার্শ্ব প্রতিক্রিয়া
});
console.log(sum); // 10
```

---

### ৩) `==` এবং `===` এর মধ্যে পার্থক্য কি?

**উত্তর:**

`==` এবং `===` উভয়ই তুলনা অপারেটর, কিন্তু ভিন্ন উপায়ে তুলনা করে:

- **`==` (Loose Equality)**: এটি শুধুমাত্র মূল্য তুলনা করে এবং প্রয়োজনে টাইপ রূপান্তর করে। এটি কম নির্ভরযোগ্য এবং অপ্রত্যাশিত ফলাফল দিতে পারে।

- **`===` (Strict Equality)**: এটি মূল্য এবং ডেটা টাইপ উভয়ই তুলনা করে। রূপান্তর করে না এবং আরও নির্ভরযোগ্য।

**উদাহরণ:**
```javascript
console.log(5 == "5");   // true (মূল্য সমান, কিন্তু টাইপ ভিন্ন)
console.log(5 === "5");  // false (টাইপ ভিন্ন)

console.log(0 == false);   // true (টাইপ রূপান্তর)
console.log(0 === false);  // false (টাইপ ভিন্ন)
```

**সুপারিশ:** সর্বদা `===` ব্যবহার করুন কারণ এটি আরও নিরাপদ এবং স্পষ্ট।

---

### ৪) API ডেটা ফেচ করার সময় `async`/`await` এর গুরুত্ব কি?

**উত্তর:**

`async`/`await` আধুনিক জাভাস্ক্রিপ্টে অ্যাসিঙ্ক্রোনাস অপারেশন সহজ এবং পরিষ্কার করে তোলে:

- **`async`**: এটি একটি ফাংশনকে চিহ্নিত করে যা প্রতিশ্রুতি (Promise) রিটার্ন করে।

- **`await`**: এটি একটি প্রতিশ্রুতির সমাধান হওয়ার জন্য অপেক্ষা করে এবং ফলাফল ফিরিয়ে দেয়।

**সুবিধাগুলি:**
1. কোড পড়া এবং বোঝা সহজ (সিঙ্ক্রোনাস কোডের মতো দেখায়)
2. ত্রুটি পরিচালনা সহজ (try/catch ব্যবহার করে)
3. অঙ্গভঙ্গি পিরামিড এড়ানো (callback hell)

**উদাহরণ (এই প্রকল্পে ব্যবহৃত):**
```javascript
async getProducts(category = 'All') {
    try {
        let url = this.baseUrl;
        if (category !== 'All') {
            const categorySlug = this.formatCategory(category);
            url = `${this.baseUrl}/category/${categorySlug}`;
        }
        const response = await fetch(url);
        const data = await response.json();
        // পণ্য প্রক্রিয়াকরণ
        return data;
    } catch (error) {
        console.error('Error fetching products:', error);
    }
}
```

---

### ৫) জাভাস্ক্রিপ্টে স্কোপ (Scope) এর ধারণা ব্যাখ্যা করুন

**উত্তর:**

স্কোপ নির্ধারণ করে যে প্রোগ্রামের কোন অংশে একটি ভেরিয়েবল অ্যাক্সেসযোগ্য। তিন ধরনের স্কোপ রয়েছে:

- **গ্লোবাল স্কোপ (Global Scope)**: ভেরিয়েবল যা কোনো ফাংশনের বাইরে ঘোষণা করা হয়। সেগুলি প্রোগ্রামের যেকোনো জায়গা থেকে অ্যাক্সেসযোগ্য।

- **ফাংশন স্কোপ (Function Scope)**: ভেরিয়েবল যা একটি ফাংশনের ভিতরে ঘোষণা করা হয়। সেগুলি শুধুমাত্র সেই ফাংশনে অ্যাক্সেসযোগ্য।

- **ব্লক স্কোপ (Block Scope)**: `let` এবং `const` দিয়ে ঘোষণা করা ভেরিয়েবল একটি ব্লকের মধ্যে সীমাবদ্ধ (উদাহরণ: `if`, `for`, `while`)।

**উদাহরণ:**
```javascript
let globalVar = "গ্লোবাল"; // গ্লোবাল স্কোপ

function myFunction() {
    let functionVar = "ফাংশন"; // ফাংশন স্কোপ
    
    if (true) {
        let blockVar = "ব্লক"; // ব্লক স্কোপ
        console.log(blockVar); // "ব্লক" (অ্যাক্সেসযোগ্য)
    }
    console.log(blockVar); // Error: blockVar অ্যাক্সেসযোগ্য নয়
}

console.log(globalVar); // "গ্লোবাল" (অ্যাক্সেসযোগ্য)
console.log(functionVar); // Error: functionVar অ্যাক্সেসযোগ্য নয়
```

---

## 🛠️ প্রযুক্তি স্ট্যাক

- **Frontend**: HTML5, CSS3 (Tailwind + DaisyUI)
- **JavaScript**: Vanilla JS (কোনো ফ্রেমওয়ার্ক নেই)
- **API**: Fake Store API
- **স্টোরেজ**: LocalStorage
- **আইকন**: Font Awesome 7

---

## 📂 ফাইল কাঠামো

```
assignment-01/
├── index.html          # হোম পেজ
├── products.html       # পণ্য পেজ
├── script.js           # মূল JavaScript ফাইল
├── README.md           # এই ফাইল
├── Assets/             # ছবি এবং সম্পদ
└── Styles/
    └── style.css       # কাস্টম CSS
```

---

## 🚀 ব্যবহার করার উপায়

1. **হোম পেজ খুলুন** (`index.html`) - ব্যানার, বৈশিষ্ট্য এবং ট্রেন্ডিং পণ্য দেখুন
2. **পণ্য পেজ খুলুন** (`products.html`) - সমস্ত পণ্য দেখুন এবং বিভাগ দ্বারা ফিল্টার করুন
3. **বিভাগ নির্বাচন করুন** - বোতামগুলিতে ক্লিক করুন যাতে পণ্যগুলি ফিল্টার হয়
4. **বিস্তারিত দেখুন** - "Details" বাটনে ক্লিক করুন পূর্ণ তথ্য জন্য
5. **কার্টে যোগ করুন** - "Add" বাটনে ক্লিক করুন
6. **কার্ট দেখুন** - নেভবার কার্ট আইকনে ক্লিক করুন

---

## 📋 API এন্ডপয়েন্ট

1. **সব পণ্য**: `https://fakestoreapi.com/products`
2. **সব বিভাগ**: `https://fakestoreapi.com/products/categories`
3. **বিভাগ অনুযায়ী পণ্য**: `https://fakestoreapi.com/products/category/{categorySlug}`
4. **একটি পণ্যের বিবরণ**: `https://fakestoreapi.com/products/{id}`

---

## 💡 প্রধান বৈশিষ্ট্যের বর্ণনা

### কার্ট ম্যানেজমেন্ট
- পণ্য যোগ এবং সরানো
- স্বয়ংক্রিয় মোট গণনা
- LocalStorage এ ডেটা সংরক্ষণ
- পেজ রিলোড করার পরেও কার্ট ধরে থাকে

### ডায়নামিক API ইন্টিগ্রেশন
- রিয়েল-টাইম বিভাগ লোডিং
- পণ্য ফিল্টারিং এবং প্রদর্শন
- লোডিং সূচক সহ ভালো UX

### রেসপন্সিভ ডিজাইন
- মোবাইল, ট্যাবলেট এবং ডেস্কটপে সম্পূর্ণ সমর্থন
- টাচ-বান্ধব ইন্টারফেস

---

## 📝 গিটহাব রিপোজিটরি

আপনার রিপোজিটরি লিঙ্ক এখানে যোগ করুন:
```
https://github.com/YOUR_USERNAME/assignment-01
```

---

## 🎓 শিক্ষামূলক মূল্য

এই প্রকল্পটি নিম্নলিখিত বিষয়গুলি শেখায়:
- DOM ম্যানিপুলেশন
- Event হ্যান্ডলিং
- Async/Await এবং API ফেচিং
- LocalStorage ব্যবহার
- মোডার্ন CSS (Tailwind)
- ক্লাস-ভিত্তিক JavaScript
- Event লিসেনার এবং ডেলিগেশন

---

## 📄 লাইসেন্স

এই প্রকল্পটি শিক্ষামূলক উদ্দেশ্যের জন্য তৈরি।

---

**সর্বশেষ আপডেট:** ১৭ ফেব্রুয়ারি, ২০২৬

